version : '3'
services:
        python-anaconda:
                image : netsarang/python3-anaconda

                volumes:
                        - ./build/:/root/build/
                ports:
                         - "1-65535:1-65535"

                container_name:
                        python-anaconda
                command:
                        /bin/bash -c "sh /root/build/pip-install.sh ; sleep infinity"
